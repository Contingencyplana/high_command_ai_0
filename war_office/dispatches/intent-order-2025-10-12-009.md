# War Office Intent Note — Order 2025-10-12-009

Issued by: War Office (Civilian Oversight)
Date: 2025-10-14

Purpose
- Ensure the composite telemetry quilt export enables downstream teams while upholding Love, Light, Safety, and Sanity.
- Prevent scope drift; codify constraints and acceptance criteria.

Scope
- Author schema documentation at `planning/toyfoundry/telemetry/export_schema.md`.
- Produce machine-readable exports (JSON, CSV) at `.toyfoundry/telemetry/quilt/exports/`.
- Automate regeneration via `tools/telemetry/quilt_loom.py` flag in lockstep with rollups.
- Provide samples and `README.md` usage for consumers.

Guardrails
- Privacy: No PII; apply redaction/aggregation; minimum-necessary fields only.
- Schema: Versioned (`v1`, `v1.1`); document field types, nullability, lineage, and units.
- Reproducibility: Deterministic generation; include `build_info.json` with commit, time, and parameters; checksum artifacts.
- Access control: Exports are internal-only; include sensitivity label and retention notes in `README.md`.
- Freshness: Document cadence (e.g., per-run/on-demand) and SLA; include timestamps in exports.
- Compatibility: Provide migration notes when schema changes; avoid breaking changes without deprecation window.

Acceptance Criteria
- `export_schema.md` reviewed by toysoldiers_ai_0 and High Command; captures fields, datatypes, and semantics.
- Export artifacts present: `export.json`, `export.csv`, and `build_info.json` under the exports folder with checksums.
- `quilt_loom.py` (or companion) supports a `--emit-exports` flag documented in `README.md`.
- Sample files and a consumer walkthrough are committed alongside the exports.
- Factory acknowledgment and completion report filed with integration notes and any follow-on requests.

Coordination
- toysoldiers_ai_0: Confirm consumer contract, provide example ingestion, and validate sample data.
- High Command: Verify acceptance criteria and sign-off.

Dependencies
- order-2025-10-12-008 appears completed (acknowledgement and archived report present). Proceed while remaining alert to upstream changes.

Risks & Mitigations
- Schema drift: Lock with versioning and review gates; add JSON Schema.
- Privacy leaks: Field audit + unit tests for redaction; include automated lints.
- Fragile automation: Add dry-run and idempotency; log failures with actionable messages.

Motto
- “We keep the machine kind.”

