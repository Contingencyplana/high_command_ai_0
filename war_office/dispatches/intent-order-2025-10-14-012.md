# War Office Intent Note - Order 2025-10-14-012

Issued by: War Office (Civilian Oversight)
Date: 2025-10-14

Purpose
- Automate and version the telemetry quilt exports so they remain reproducible, verifiable, and consumer-friendly over time.

Scope
- Integrate export generation into the rollup pipeline (e.g., `tools/telemetry/quilt_loom.py --export`).
- Add versioned schema metadata (`schema_version: "1.0"`) and author `exports/README.md` with sensitivity, retention, cadence, and usage.
- Emit checksums (`*.sha256`) and `build_info.json` per artifact.
- Document a simple CI routine to regenerate and verify exports on `main`.

Guardrails
- Privacy: No PII; retain only minimum-necessary fields; document sensitivity.
- Reproducibility: Deterministic runs; include commit/time/params in `build_info.json`.
- Stability: Avoid breaking changes; introduce schema updates with deprecation notes.
- Operability: Clear failure messages; dry-run mode and idempotent behavior.

Acceptance Criteria
- `composite_export.json` and `.csv` regenerated via a documented flag; checksums present and validated.
- `exports/README.md` and `build_info.json` exist with required details.
- Schema version explicitly included; consumer guidance present.
- Factory ACK + completion report filed with metrics and paths.

Coordination
- toysoldiers_ai_0: confirm contract and sample ingestion.
- High Command: review README, schema versioning, and automation notes.

Dependencies
- order-2025-10-12-009, order-2025-10-14-010

Motto
- "We keep the machine kind."
