# Manifests — High-Rank (K–P) Summaries

This folder is generated by `tools/manifest_generator.py`.

- Input roots (default):
  - `exchange/orders/completed/`
  - `exchange/reports/archived/`
  - `contract_samples/cases/`
- Output files:
  - `index.json` — overview of available ranks and counts
  - `{kilo|lima|mike|november|oscar|papa}.json` — identifier counts by rank

Regenerate:

```bash
python tools/manifest_generator.py \
  --roots exchange/orders/completed exchange/reports/archived contract_samples/cases \
  --out high_command_ai_0/manifests
```

Notes
- Files without `rank_path` are skipped.
- This is a minimal stub; extend as needed (e.g., workspace/juliett rollups, nested trees).
